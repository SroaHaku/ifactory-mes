spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/mes_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456

# JWT配置
jwt:
  secret: mesFrameworkSecretKey123456
  expiration: 3600000  # 1小时

# 文件上传配置
file:
  upload-path: D:\FILE
  chunk-size: 10485760  # 10MB

server:
  port: 8081
  max-http-header-size: 102400  # 必须添加，否则会覆盖主配置
  tomcat:
    max-http-form-post-size: -1

IP: http://192.168.2.48:9527
