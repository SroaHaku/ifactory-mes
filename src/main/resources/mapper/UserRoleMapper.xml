<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mes.mapper.UserRoleMapper">
    <!-- 通过用户ID查询角色编码 -->
    <select id="selectRoleCodesByUserId" parameterType="java.lang.Long" resultType="java.lang.String">
        SELECT r.role_code
        FROM sys_user_role ur
                 LEFT JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
    </select>

    <!-- 查询用户关联的角色：通过 sys_user_role 中间表关联 sys_role -->
    <select id="selectRolesByUserId" resultType="java.lang.String">
        SELECT
        sr.role_name AS roleName
        FROM
        sys_role sr
        LEFT JOIN
        sys_user_role sur ON sur.role_id = sr.id
        WHERE
        sur.user_id = #{userId}
    </select>

    <insert id="batchInsert">
        INSERT INTO sys_user_role (user_id, role_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.roleId})
        </foreach>
    </insert>

    <select id="selectRolesIdByUserId" resultType="java.lang.Long">
        SELECT
        sr.id AS roleId
        FROM
        sys_role sr
        LEFT JOIN
        sys_user_role sur ON sur.role_id = sr.id
        WHERE
        sur.user_id = #{userId}
    </select>
</mapper>
