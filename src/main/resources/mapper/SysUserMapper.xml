<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mes.mapper.SysUserMapper">
    <select id="selectUserByUsername" resultType="com.mes.entity.SysUser">
        SELECT id, username, password, real_name AS realName, phone, status,
               create_time AS createTime, update_time AS updateTime, del_flag AS delFlag
        FROM sys_user
        WHERE username = #{username} AND del_flag = 0
    </select>

</mapper>